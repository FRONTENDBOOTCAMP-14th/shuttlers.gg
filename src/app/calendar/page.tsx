import { Calendar } from '@/app/calendar/Calendar';

export const metadata = {
  title: 'Calendar | Shuttlers',
  description: '배드민턴 대회 캘린더',
};

export default function CalendarPage() {
  return <Calendar />;
}
